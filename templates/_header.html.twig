
<header class="navbar-nav navbar-expand-lg">
    <nav class="container-fluid ">
        <div class="row align-items-center">

            <div class="col col-lg-1 nav-item">
                <i class="fa-solid fa-bars fa-4x"></i>
            </div>

            <div class="col col-sm-3 col-md-5 col-lg-6 nav-item text-white">
                <a href="{{ path('/accueil') }}"><h1>Facile'eat la vie</h1></a>
            </div>

            <div class="col  col-lg-5 d-flex justify-content-end" id="containerBtnHeader">
                <div class="text-center d-flex flex-row justify-content-center pt-4 logo">
                    <div class="text-center d-flex flex-column justify-content-center pt-4" id="containerSvgHeader">
                        <a href="{{ path('/panier') }}"><i class="fas fa-shopping-cart fa-3x"></i></a>
                        <p>Panier</p>
                    </div>
                </div>

                    {% if is_granted('ROLE_SUPER_ADMIN') %}
                    <div class="text-center d-flex flex-row justify-content-center pt-4 logo">
                        <div class="text-center d-flex flex-column justify-content-center pt-4" id="containerSvgHeader">
                            <a href="{{ path('admin') }}"><i class="fa fa-wrench fa-3x"></i></a>
                            <p>Administration</p>
                        </div>
                    </div>
                    {% endif %}


                    {% if app.user is null %}
                    <div class="text-center d-flex flex-row justify-content-center pt-4 logouser">
                        <a href="{{ path('app_register')}}" class="btn btn-lg bg-dark text-white mx-2 btnHeader">Inscription</a>
                        <a href="{{ path('app_login') }}" class="btn btn-lg  bg-white text-dark mx-2 btnHeader">Connexion</a>
                    </div>


                    {% else %}
                        {# Vous êtes connecté en tant que {{ app.user.userIdentifier }}. #}
                        <div class="text-center d-flex flex-column justify-content-center pt-4">
                            <div class="text-center d-flex flex-column justify-content-center pt-4" id="containerSvgHeader">
                            <i class="fa fa-user-circle fa-3x"></i>
                            <p>{{ app.user.userIdentifier }}</p>
                            </div>
                        </div>
                            
                        <div class="text-center d-flex flex-column justify-content-center pt-4 logo">
                            <div class="text-center d-flex flex-column justify-content-center pt-4" id="containerSvgHeader">
                            <a href="{{ path('app_logout') }}"><i class="fa fa-window-close fa-3x"></i></a>
                            <p>Déconnexion</p>
                            </div>
                        </div>
                    {% endif %}
                </div>


                    

            </div>
        </div>

    </nav>
</header>

<div class="burger " id="burger">
    <div class="container">
        <div class="row orange">
            <div class="col ">
                <h1 class="ml-2">Menu</h1>
            </div>
            <div class="col d-flex justify-content-end">
                <i class="fas fa-times fa-3x"></i>
            </div>
        </div>
        <div class="container mt-5">
            <div class="row flex-column align-items-end">
                <a class="h4 mt-3 text-center burger-item" href="{{ path('/accueil') }}">Accueil</a>
                <a class="h4 mt-3 text-center burger-item" href="{{ path('/connexion') }}">Connexion</a>
                <a class="h4 mt-3 text-center burger-item" href="{{ path('/livraison') }}">Livraison</a>
                <a class="h4 mt-3 text-center burger-item" href="{{ path('contact') }}">Messagerie</a>
                <a class="h4 mt-3 text-center burger-item" href="{{ path('/panier') }}">Panier</a>
                <a class="h4 mt-3 text-center burger-item" href="{{ path('/apropos') }}">À propos</a>
            </div>
        </div>
    </div>
</div>
